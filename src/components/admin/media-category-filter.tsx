"use client"

// Wave 0 stub — Wave 2 (07-02) will implement this component (MEDIA-06).
// Exported so tests can import it; renders nothing meaningful until Wave 2 lands.

export function MediaCategoryFilter({ activeCategory: _activeCategory }: { activeCategory: string }) {
  // Wave 2 will render category pills (All, project, profile, experience, education, reference)
  // with router.push navigation and active-state highlighting.
  return null
}
